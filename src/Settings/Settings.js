import React from "react";

export const Settings = () => {
    return (
        <section>
            <h2>Settings</h2>
            <div>
                TODO: Add the settings.
            </div>
        </section>
    );
};